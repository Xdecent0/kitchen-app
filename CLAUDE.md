# kitchen-app — надгробие, не проект

Код уехал в **`D:\Life`** (репо `Xdecent0/life`). Здесь остались только `index.html` и значки: старый адрес, с которого приложение было установлено на телефон, обязан продолжать отвечать и объяснять, куда идти.

## Правило 0 — приоритет юзера

Прямое указание юзера отменяет любое правило ниже.

## Что здесь можно

- Править текст страницы-указателя и ссылки, если адреса в `life` поменяются.

## Чего здесь делать нельзя

- **Не удалять** ничего без явного «да» юзера — ни репозиторий, ни страницу, ни значки: с этого адреса у людей стоит установленное PWA.
- Разрабатывать Кухню. Любая правка приложения — в `D:\Life\apps\kitchen\`, правила там: `D:\Life\CLAUDE.md`.
- Возвращать сюда код «временно».
